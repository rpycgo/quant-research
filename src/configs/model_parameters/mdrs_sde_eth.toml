# configs/model_parameters/mdrs_sde_eth.toml
#
# Local override for the MDRS-SDE model on ETHUSDT futures.
# Only keys that deviate from mdrs_sde/configs/default_config.toml.

[sde_priors]
gamma_mu    = 3.0
gamma_lower = 1.5

[mcmc_settings]
draws = 2000
